{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "pluribus:credential/static-plugin@1",
  "title": "Secret bytes a plugin reads itself",
  "description": "The enrolled input is sealed under the credential handle and returned by credentials.get to components the manifest grants access. The host injects nothing, so the component holds whatever authority the secret carries and places it in the protocol itself.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "components": {
      "description": "Informational. Access is decided by the manifest's credential declaration, not by this flow.",
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    }
  }
}
