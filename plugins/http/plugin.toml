abi = "pluribus:plugin@1.0.0"
manifest_version = 1
id = "dev.pluribus.http"
name = "HTTP"
config_schema = "config.schema.json"
license = "MIT OR Apache-2.0"

[components.listen]
world = "pluribus:plugin/source@1.0.0"
component = "plugin.wasm"
digest = "sha256:dev"
config_schema = "config.schema.json"
imports = ["pluribus:plugin/events@1.0.0", "pluribus:plugin/reader@1.0.0", "pluribus:plugin/socket@1.0.0", "pluribus:plugin/state@1.0.0", "pluribus:plugin/writer@1.0.0"]
subscribes = ["http.response.requested"]
emits = ["http.request.received"]

[[components.listen.requested_capabilities]]
name = "host.stream"
required = true
reason = "Connect to the configured native service"
constraints = { unrestricted = true }
